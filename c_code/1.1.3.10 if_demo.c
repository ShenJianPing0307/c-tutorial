#define _CRT_NO_SECURE_WARNINGS
#include<stdio.h>
#include<stdlib.h>
/*
int main() {
	int a = 2;
	int b = 3;
	//if后面不要加 分号,分号是空语句
	//一般if后面加上{},{}里面的语句都属于if
	//如果if后面不加{},只有if后面的一条语句属于if
	if (a == 2) {
		printf("%d\n", b);
	}
	system("pause");
	return 0;
}
*/